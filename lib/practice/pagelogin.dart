import 'package:flutter/material.dart';

class Finance extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return const Scaffold(
      body: Center(
        child: Text(
          "Hello Flutter",
          style: TextStyle(fontSize: 24, fontFamily: "CroissantOne"),
        ),
      ),
    );
  }
}
